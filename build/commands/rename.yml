name: rename
multi-args:
   sources: { optional: false, alias: source }
single-args:
   dest: ~
multi-opts:
   include: { cli: '-I', alias: incl }
   exclude: { cli: '-X', alias: excl }
flags:
   after:  { cli: '-A' }
   force:  { cli: '-f' }
   dryRun: { cli: '-n' }
