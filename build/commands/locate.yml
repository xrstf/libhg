name: locate
multi-args:
   patterns: { alias: pattern }
multi-opts:
   include: { cli: '-I', alias: incl }
   exclude: { cli: '-X', alias: excl }
single-opts:
   rev: { cli: '-r' }
flags:
#  print0:   { cli: '-0' } # useless in our usecase
   fullpath: { cli: '-f' }
