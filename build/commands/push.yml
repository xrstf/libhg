name: push
single-args:
   dest: ~
multi-opts:
   revs:      { cli: '-r', alias: rev      }
   bookmarks: { cli: '-B', alias: bookmark }
   branches:  { cli: '-b', alias: branch   }
flags:
   force:     { cli: '-f' }
   newBranch: ~
   insecure:  ~
