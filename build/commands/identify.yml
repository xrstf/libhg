name: identify
single-args:
   source: ~
single-opts:
   rev:       { cli: '-r'          }
   ssh:       { cli: '-e'          }
   remoteCmd: { cli: '--remotecmd' }
flags:
#  num:       { cli: '-n' }  # always on
#  id:        { cli: '-i' }  # always on
#  branch:    { cli: '-b' }  # always on
#  tags:      { cli: '-t' }  # unsupported because there is no good way to parse output
#  bookmarks: { cli: '-B' }  # unsupported because there is no good way to parse output
   insecure:  ~
